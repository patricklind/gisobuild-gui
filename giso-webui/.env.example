# Copy this file to .env for local configuration. Used by compose.yaml (the
# default, self-contained deployment) and compose.socket.yaml.
LOG_LEVEL=INFO

# Leave empty to build the default image locally (docker compose up -d --build).
# Set it to a published image to pull instead, e.g.
# GISO_WEBUI_IMAGE=ghcr.io/patricklind/gisobuild-gui:latest

# Optional Cisco Automated Software Distribution API credentials.
# Prefer *_FILE paths backed by Docker secrets instead of literal values.
CISCO_CLIENT_ID=
CISCO_CLIENT_SECRET=
CISCO_SECRETS_DIR=./secrets
CISCO_CLIENT_ID_FILE=/run/secrets/cisco_client_id
CISCO_CLIENT_SECRET_FILE=/run/secrets/cisco_client_secret
CISCO_DOWNLOAD_TIMEOUT_SECONDS=60
CISCO_DOWNLOAD_HOSTS=cisco.com

# Upload and extraction limits
MAX_UPLOAD_BYTES=8589934592
MAX_EXTRACTED_BYTES=17179869184
MAX_TAR_MEMBERS=10000
MAX_CHUNK_BYTES=16777216
MAX_LOG_BYTES=10485760
MAX_JOB_HISTORY=100
UPLOAD_SESSION_TTL=86400

# Archive policy: retain artifacts for 30 days, up to 50 GiB total
ARCHIVE_RETENTION_DAYS=30
MAX_ARCHIVE_BYTES=53687091200
ARCHIVE_CLEANUP_INTERVAL_SECONDS=3600

# Keep the service local unless you intentionally configure a reverse proxy
ALLOWED_HOSTS=127.0.0.1,localhost,giso-webui
WEB_BIND_ADDRESS=127.0.0.1
WEB_PORT=8080

# Only for compose.socket.yaml (Docker socket + Cisco builder image). The
# default deployment bundles gisobuild and ignores these.
# GISO_IMAGE=ciscogisobuild/cisco-xr-gisobuild:2.3.4@sha256:be282c7a76b03820d7bdd6c8b8cc0d4a54a5b6207143f089123b32e245018bf3
# GISO_PULL_TIMEOUT_SECONDS=600
# Container storage paths (compose.socket.yaml; change only together with its mounts)
# DATA_ROOT=/uploads
# OUTPUT_ROOT=/output
# TOOL_ROOT=/tool
# WORK_ROOT=/work
# ARCHIVE_ROOT=/archive
# STATE_ROOT=/state
