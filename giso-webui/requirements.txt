# Every installed distribution is pinned, transitive ones included, so two
# builds of the same commit install identical Python code. Regenerate from the
# built image (`pip freeze`) when changing a direct dependency.
Flask==3.1.3
click==8.5.0
gunicorn==23.0.0
# Transitive (Flask, gunicorn)
blinker==1.9.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.3
Werkzeug==3.1.8
